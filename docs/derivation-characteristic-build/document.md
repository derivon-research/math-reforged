# 用广义特征空间定义特征多项式

设复算子 $T$ 的不同特征值为 $\lambda_j$，令 $d_j=\dim G(\lambda_j,T)$。定义

$$
p_T(z)=\prod_j(z-\lambda_j)^{d_j}.
$$

这一步只构造特征多项式；证明 $p_T(T)=0$ 属于独立的 Cayley-Hamilton 节点。

> 教材依据：第 8C 节，定义 8.23-8.28。权重 2.0：组合广义特征空间维数与有限乘积；置信度高。
