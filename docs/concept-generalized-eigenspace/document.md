# 广义特征空间

设 $T\in\mathcal L(V)$、$\lambda$ 为特征值且 $n=\dim V$。广义特征空间定义为

$$
G(\lambda,T)=\operatorname{null}(T-\lambda I)^n.
$$

在该空间上，$T-\lambda I$ 的限制是幂零算子；这是二者的关系，不是把它们合成一个概念。

> 教材对应：第 8B 节，8.10-8.22。
