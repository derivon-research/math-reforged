# 从正向计算转为求解 $Ax=b$

矩阵-向量乘积先回答“给定 $x$，输出 $b=Ax$ 是什么”。把 $A,b$ 固定而把 $x$ 作为未知量，同一等式就成为线性方程组。列视角要求寻找产生 $b$ 的列组合，行视角给出逐行标量方程。

**权重：1.0。** 计算规则不变，只交换已知量与未知量的角色，是一次直接的问题重述。
+
> 来源：Strang, 5th ed., §1.3, pp. 23-24。
